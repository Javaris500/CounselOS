/**
 * Login. The form lands in 0b with Module 2 — it needs Supabase Auth to submit
 * against, and a login form that cannot log in is worse than an honest stub.
 */
export default function LoginPage(): React.JSX.Element {
  return (
    <main style={{ maxWidth: '52ch', margin: '0 auto', padding: '2.5rem 1.5rem' }}>
      <h1>Sign in</h1>
      <p>Authentication arrives with Module 2.</p>
    </main>
  );
}
